function G = planefitguess

%Double check Initial guesses

global RF;

xslopeguess = std(f(:))/std(xpts);
yslopeguess = std(f(:))/std(ypts);
baseguess = 

%G = [idx length(f)/6 max(RF)-min(RF) min(RF)];
G = [idx sig ma-mi mi];
%G = [length(f)/2 1 0 0];